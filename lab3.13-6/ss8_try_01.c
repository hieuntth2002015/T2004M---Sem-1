#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int x,y;
	printf(" Nhap gia tri x : ");
	scanf("%d", &x);
	printf(" Nhap gia tri y : ");
	scanf("%d", &y);
	{
	if 
	(x > 2000 && x < 3000)
	printf(" gia tri cua x la %d", x);
	else
	printf(" Khong the hien thi gia tri cua x\n");
}
    {
	if (y >100 && y<500)
	printf(" Gia tri cua y la %d", y);
	else 
	printf(" Khong the hien tri gia tri cua y\n");
}
	return 0;
}
