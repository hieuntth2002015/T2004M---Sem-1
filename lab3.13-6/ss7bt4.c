#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int luong;
	printf(" Nhap muc luong cua nhan vien : ");
	scanf("%d", &luong);
	
    if (luong=300)
     printf(" Nhan vien cap bac A");
    else if (luong= 250)
    printf(" Nhan vien Cap bac B");
    else if (100)
     printf(" Nhung loai nhan vien khac ");
	return 0;
}
