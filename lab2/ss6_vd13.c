#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

void main() {
	char letter;
	printf(" Ban co the nhap so nguyen ngay bay gio :");
	letter = getchar();
	putchar(letter);
	
}
